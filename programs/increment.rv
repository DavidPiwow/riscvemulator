
loop:
    lw   x3, 0(x1)        # load memory[0]
    addi  x3, x3, 1       # x3 += 1
    sw   x3, 0(x1)        # store it back
    addi x4, x0, 10       # limit = 10
    blt  x3, x4, loop     # repeat if less than 10
    ebreak
